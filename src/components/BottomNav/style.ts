import { Theme } from "@mui/system";
import { makeStyles } from "@mui/styles";

export default makeStyles((theme: Theme) => ({
    root: {
        position: "absolute",
        bottom: 0,
        width: "100%",
    }
})) 